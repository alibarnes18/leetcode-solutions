# 2828. Check if a String Is an Acronym of Words

This repository contains a Python solution for the LeetCode problem:

**2828 - Check if a String Is an Acronym of Words**

## Problem Description

Given an array of strings `words` and a string `s`, determine whether `s` is an acronym formed by concatenating the first character of each word in `words`.

### Example

Input:

```python
words = ["alice", "bob", "charlie"]
s = "abc"